/**
 * Generate `public/llms.txt` from the Figma-exported token JSON files.
 *
 * Runs in `prebuild` (before `vite build`) and `predev` (before `vite`) so the
 * file is always in sync with the source tokens. Outputs plain text with
 * `Content-Type: text/plain` semantics — AI agents can fetch the URL directly
 * (no JS execution required).
 */

import { copyFileSync, readFileSync, writeFileSync, mkdirSync } from "node:fs";
import { resolve, dirname } from "node:path";
import { fileURLToPath } from "node:url";

import {
  flattenColor,
  flattenSize,
  flattenFont,
  extractBreakpointPx,
  diffFromBase,
  formatTokenLines,
  buildBootstrapCss,
  buildBootstrapShim,
  buildBreakpointsJs,
} from "../supabase/functions/_shared/token-generators.mjs";

const __dirname = dirname(fileURLToPath(import.meta.url));
const ROOT = resolve(__dirname, "..");

function readJson(rel) {
  return JSON.parse(readFileSync(resolve(ROOT, rel), "utf-8"));
}

// ── Build llms.txt ─────────────────────────────────────────────────────────

// Public origin where the built site is served. AI agents fetch llms.txt
// from this origin and follow links inside it to logos, skills MDs, etc.
// Single source of truth: change here, re-run prebuild, redeploy.
const SITE_BASE_URL = "https://design-system.arcsite.com";

// Live token artifacts are served from Supabase Storage and regenerated by the
// CMS on every "Publish to Production" (decision #12) — so token edits reach
// agents + prototypes instantly, no rebuild. The stable
// design-system.arcsite.com/tokens/bootstrap.css is a 1-line @import shim
// forwarding here (back-compat for that URL); llms.txt points agents at these
// Storage URLs directly. projectId resolves like safe-changelog-sync.mjs
// (env → info.tsx fallback).
const INFO_SRC = readFileSync(resolve(ROOT, "utils/supabase/info.tsx"), "utf-8");
const SUPABASE_PROJECT_ID =
  process.env.VITE_SUPABASE_PROJECT_ID ||
  INFO_SRC.match(/projectId\s*=[^"]*"([^"]+)"/)?.[1];
if (!SUPABASE_PROJECT_ID) {
  throw new Error("Could not resolve Supabase projectId from env or utils/supabase/info.tsx");
}
const STORAGE_BASE = `https://${SUPABASE_PROJECT_ID}.supabase.co/storage/v1/object/public/design-tokens`;

// Icons are served LIVE from the CMS edge function (decision #13) — the same
// KV-read model patterns use for their `.md`. An icon uploaded in the CMS is
// reachable at these URLs within the route's ~60s cache, with no rebuild or
// publish step. Egress note matches tokens (decision #12): these are
// *.supabase.co URLs; a consumer behind an egress allowlist that excludes
// Supabase must allowlist the host.
const EDGE_BASE = `https://${SUPABASE_PROJECT_ID}.supabase.co/functions/v1/make-server-067f252d`;

// prebuild = production deploy build; predev / one-off runs = local dev.
const PRODUCTION_BUILD = process.env.npm_lifecycle_event === "prebuild";

const lightTokens  = flattenColor(readJson("tokens/color/light.tokens.json"));
const darkTokens   = flattenColor(readJson("tokens/color/dark.tokens.json"));
const globalColor  = flattenColor(readJson("tokens/color/color-global-value.tokens.json"));
const fontDesktop  = flattenFont(readJson("tokens/font/web-desktop.tokens.json"));
const sizeGlobal   = flattenSize(readJson("tokens/size/size-global-value.tokens.json"));

// Web modes drive the responsive cascade. Order is mobile-first so the
// CSS reads: :root = Web Mobile → @media tablet → @media desktop → @media
// desktop-large. Device modes (mobile/tablet) are *also* emitted now —
// see the device-* diffs below. They're triggered explicitly by a class
// on <html> for prototypes that need to emulate native iOS / Android.
const sizeWebMobile       = flattenSize(readJson("tokens/size/web-mobile.tokens.json"));
const sizeWebTablet       = flattenSize(readJson("tokens/size/web-tablet.tokens.json"));
const sizeWebDesktop      = flattenSize(readJson("tokens/size/web-desktop.tokens.json"));
const sizeWebDesktopLarge = flattenSize(readJson("tokens/size/web-desktop-large.tokens.json"));
const sizeDeviceMobile    = flattenSize(readJson("tokens/size/device-mobile.tokens.json"));
const sizeDeviceTablet    = flattenSize(readJson("tokens/size/device-tablet.tokens.json"));
const fontDeviceMobile    = flattenFont(readJson("tokens/font/device-mobile.tokens.json"));
const fontDeviceTablet    = flattenFont(readJson("tokens/font/device-tablet.tokens.json"));

// Breakpoints live in their own collection — single mode, mode-independent.
const breakpoints = flattenSize(readJson("tokens/breakpoint/breakpoint.tokens.json"));

// Extract raw breakpoint pixel values for media-query templates. CSS custom
// properties don't work inside @media queries, so we hard-emit the numbers.
const breakpointPx = extractBreakpointPx(readJson("tokens/breakpoint/breakpoint.tokens.json"));

// Only the tokens that differ from the :root base need to be emitted in
// each mode block — saves bytes and keeps the cascade legible. Web modes
// diff against web-mobile (since that's what :root holds for size).
// Device modes also diff against web-mobile for size; device fonts diff
// against fontDesktop because that's what :root holds for font.
const sizeTabletDiff       = diffFromBase(sizeWebMobile, sizeWebTablet);
const sizeDesktopDiff      = diffFromBase(sizeWebMobile, sizeWebDesktop);
const sizeDesktopLargeDiff = diffFromBase(sizeWebMobile, sizeWebDesktopLarge);
const sizeDeviceMobileDiff = diffFromBase(sizeWebMobile, sizeDeviceMobile);
const sizeDeviceTabletDiff = diffFromBase(sizeWebMobile, sizeDeviceTablet);
const fontDeviceMobileDiff = diffFromBase(fontDesktop, fontDeviceMobile, "value");
const fontDeviceTabletDiff = diffFromBase(fontDesktop, fontDeviceTablet, "value");

const fmt = formatTokenLines;

const content = `# Arcsite Design System
Single source of truth for Arcsite product UI.
All tokens are CSS custom properties — reference as \`var(--token-name)\`.
Tailwind 4 arbitrary value syntax: \`bg-(--color-label-primary)\`, \`text-(--color-label-primary)\`, etc.

## Bootstrap (non-optional for new prototypes / standalone UI)

Any new ArcSite UI being built from scratch (prototype, demo, marketing page, internal tool) **must** start by importing the canonical token + font bootstrap stylesheet. This loads Inter, defines every CSS variable referenced below, and sets up dark-mode swap automatically. Copy these two lines into the \`<head>\`:

\`\`\`html
<link rel="stylesheet" href="${SITE_BASE_URL}/tokens/bootstrap.css">
\`\`\`

After importing, reference tokens by name only (\`color: var(--color-label-primary);\`). Do not hand-copy values from the sections below into your CSS — those sections are reference, not source. Dark mode: add \`class="dark"\` or \`data-theme="dark"\` to \`<html>\` (or any subtree).

If you are editing inside an established codebase that already wires tokens through its own build (\`arcsite_web\`, \`proposalv3\`, etc.), follow that codebase's existing import pattern instead — do not import bootstrap.css alongside it.

---

Token resolution rule: bind layers and components to **semantic** tokens
(\`--color-label-*\`, \`--color-fill-*\`, \`--color-surface-*\`, \`--color-border-*\`,
\`--size-spacing-*\`, \`--size-padding-*\`, \`--size-radius-*\`, \`--text-*\`).
Globals (\`--color-global-*\`, \`--size-global-*\`) are primitives — only reference
them directly inside the design system itself, never in product UI.

---

## Token reference pages
- Typography tokens: /typography/tokens
- Color tokens:      /color/tokens
- Color swatches:    /color/swatches
- Size tokens:       /size/tokens

## Full token documentation
- ${STORAGE_BASE}/tokens-color.md
- ${STORAGE_BASE}/tokens-typography.md
- ${STORAGE_BASE}/tokens-size-space.md

---

## Icons — which library to use

**Rule of thumb: prefer the project's own icon library when one exists.**

1. **Working on the Arcsite user site** (\`arcsite_web\` repo)?
   Always pull icons from the project's bundled library:
   \`\`\`
   packages/arcsite-icons/icon
   \`\`\`
   (path relative to the \`arcsite_web\` repo root)
   You may still use this design system website as a *visual reference* to
   decide which icon to use, but the actual import must come from the
   \`arcsite-icons\` package — not from this design system.

2. **Working on a prototype or any project that does not have its own
   icon library**? Use the design system's icon library:
   - Visual browser:    /iconology
   - Search manifest:   ${EDGE_BASE}/icons.index.json  (slim — name + tags + size, no SVG bytes)
   - Per-icon SVG file: ${EDGE_BASE}/icons/{fileName}  (raw SVG, ~700 bytes each)
   - Full manifest:     ${EDGE_BASE}/icons.json        (every icon + inline \`svgContent\`)

   These three endpoints are served **live from the CMS** — an icon uploaded
   or retagged in the CMS shows up here within ~60 seconds, no rebuild. (The
   stable /icons.json, /icons.index.json, /icons/{fileName} on this site still
   exist as a build-time snapshot, but the live URLs above are canonical.)

   **Two-step flow (recommended for agents):**
   1. Fetch \`${EDGE_BASE}/icons.index.json\` and search by tag (e.g.
      \`tags.includes("arrow")\`), \`name\`, or \`size\` / \`height\` / \`width\`. The
      slim index is ~5× smaller than the full manifest, so it fits comfortably
      into context during the "pick an icon" phase.
   2. Once you've picked one, fetch \`${EDGE_BASE}/icons/\${entry.fileName}\` to
      get the raw SVG (~700 bytes) and drop it straight into your component.
      Use this path with \`<img src>\`, \`fetch().then(r => r.text())\`, or any
      other SVG-loading mechanism.

   You can skip the manifest and use \`${EDGE_BASE}/icons.json\` if you want the
   entire library inline in one request — but for picking a handful of icons,
   slim index → per-file fetch is far lighter.

   **Library missing an icon?** In a prototype, a third-party icon (e.g.
   Lucide) is acceptable as a *temporary stand-in* — disclose it to the user
   when you add it and track it as an open gap. Icon quality is a delivery
   gate: before final delivery, swap every stand-in for a design-system icon
   or ask the design team to add the icon to the library (uploaded via the
   CMS, live at these endpoints within ~a minute).

   Each entry has:
   - \`name\`        — kebab-case identifier (with size suffix, e.g. \`chevron right 16x10\`)
   - \`fileName\`    — original upload filename
   - \`tags\`        — search keywords (intent + visual descriptors)
   - \`size\`        — parsed size string, e.g. \`"16x10"\`
   - \`height\`      — height in px (number)
   - \`width\`       — width in px (number)
   - \`svgContent\`  — full inline SVG (full manifest only)

   **Size convention — read carefully:** the size suffix in icon names and
   the \`size\` field is **\`{height}x{width}\`** (height first, *not* the
   common width × height ordering). For example, \`chevron right 16x10\`
   is **16px tall, 10px wide**. Height is the canonical grouping dimension:
   icons in the same height bucket may have slightly different widths
   (e.g. a 16-tall arrow might be 10, 11, or 12 wide depending on the
   glyph), but they're all considered the "16-height" set and should be
   used together for visual consistency. When deciding which size to use,
   match the **height** to the surrounding text/UI density, not the width.

---

## Token values

All CSS-variable definitions live in the bootstrap stylesheet. Don't paste
numeric values inline; reference tokens by name and import the stylesheet.
These artifacts are **published live from the CMS** — a designer hitting
"Publish to Production" regenerates them within seconds, so fetch them fresh
rather than caching values (decision #12). The stable
${SITE_BASE_URL}/tokens/bootstrap.css URL is a shim forwarding to the first
URL below.

- **Tokens CSS (color + size + font + breakpoint vars, mobile-first cascade):**
  ${STORAGE_BASE}/bootstrap.css

- **Breakpoints as JS object (for matchMedia / Tailwind config / container queries):**
  ${STORAGE_BASE}/breakpoints.js

- **Token reference docs (Markdown):**
  ${STORAGE_BASE}/tokens-color.md
  ${STORAGE_BASE}/tokens-size-space.md
  ${STORAGE_BASE}/tokens-typography.md

The size cascade is mobile-first: defaults are Web Mobile, with
\`@media (min-width: ${breakpointPx.sm}px / ${breakpointPx.lg}px / ${breakpointPx.xl}px)\`
overrides for Web Tablet / Web Desktop / Web Desktop Large. Each override
block lists only tokens that differ from Web Mobile — parse \`bootstrap.css\`
directly for the full set.

CSS custom properties **do not work inside \`@media\` queries** — the
\`breakpoints.js\` module above exists for JS (\`matchMedia\`,
\`window.innerWidth\`), Tailwind config, or container queries.

---

## Design Principles (LLM-applicable)

When generating ArcSite designs or UI code, follow these rules. Each is
phrased as **Rule / Why / How to apply** — apply them in order before falling
back to your own judgment.

### 1. Component namespace must not cross scopes
- **Rule** — Library components are scoped by **name prefix** (primary truth). The Figma section is only a fallback when a component has no prefix.
  - \`app/*\` → ArcSite webapp only (in-product drawing / proposal UI)
  - \`web/*\` → user-site / marketing web only
  - **Unprefixed → shared primitive.** Includes design tokens (\`color/*\`, \`size/*\`, \`font/*\`), the main icon library (the \`Icons\` and \`Icons-Source\` pages, plus shared icon families like \`icon 48/*\`, \`icon 30/*\`), navigation primitives (\`navigation-item/*\`), shared media (\`image/*\`, \`logo-arcsite/*\`, \`cursor/*\`, \`ar-icons\`, placeholder variants like \`default img\`, \`default video\`, \`low-fi/img\`), and illustrations on the \`Image\` page.
  - When prefix is absent, fall back to the enclosing Figma section: \`app\` section → app-only, \`web\` section → web-only, no scoped section → shared.
- **Why** — The two products have different visual density and language; mixing breaks system intent and produces designs that need rework. Prefix-as-truth makes scope decidable from the component name alone — no need to inspect Figma section context at consumption time.
- **How to apply** — Before instantiating, read the prefix. When \`search_design_system\` returns multiple variants of the same logical component (e.g. \`app/table/row\` vs \`web/table/row\`), pick the one matching the current project. If no match exists for your project's namespace, flag to the user — never force a wrong-namespace component.

### 2. Default interaction color is action blue; onboarding is the only exception
- **Rule** — Primary interactive color (primary buttons, active nav, links, chart emphasis, focus ring):
  - All products (user-site, marketing web, webapp interior) → \`action\` token family (blue)
  - **Onboarding flows** (regardless of which product they live in) → \`brand\` token family (orange)
  - Logo / brand-identity surfaces (splash, brand storytelling, empty-state illustrations) → \`brand\` token family
- **Why** — The product's default interactive language is cool/blue (matches the \`action\` token semantic). Brand orange reads as warm/promotional and is reserved for moments where brand identity is the message — most importantly the user's first encounter via onboarding.
- **How to apply** —
  - Onboarding: primary CTA, active step, focus ring all bind to \`brand/primary\`; container uses \`surface/container/brand\`. Do not mix in \`action\` tokens.
  - Everywhere else: active nav row = \`surface/container/action\` + \`label/action/primary\`; chart series primary/secondary = \`label/action/primary\` / \`…/secondary\`.
  - Logo SVGs: reference \`${SITE_BASE_URL}/logos/glyph*.svg\` directly — they bake in brand orange.

### 3. Empty input placeholders bind to tertiary
- **Rule** — When an input is empty, the placeholder text binds to \`color/label/tertiary\` (or \`…/tertiary-increased-contrast\`). Never \`primary\` or \`secondary\`.
- **Why** — Tertiary is the system's "low-emphasis hint" semantic. Primary makes empty inputs look pre-filled (misleading); secondary is for sub-labels, not placeholders.
- **How to apply** — All input components default placeholder = tertiary. Filled state = \`label/primary\` / \`…/primary-increased-contrast\`. The placeholder ↔ filled toggle is exactly tertiary ↔ primary.

### 4. Active / selected states use intent surface + intent label
- **Rule** — For "currently selected" / "highlighted" states, compose \`surface/container/{intent}\` as background and \`label/{intent}/primary\` as foreground. Do not roll your own tinted colors or fade the brand color manually.
- **Why** — Container tokens are pre-tuned so foreground text remains legible; ad-hoc tints break the brightness system and don't follow dark-mode swaps.
- **How to apply** —
  - User-site, webapp default → \`intent=action\`
  - Onboarding → \`intent=brand\` (see principle 2)
  - Status states → \`intent=caution / danger / success\`
  - Brand-tinted promo / splash cards → \`intent=brand\`

### 5. Ghost / chrome-less inputs only when external chrome already frames them
- **Rule** — Input \`style=ghost\` (no border, no trailing button) is allowed only when an external container already provides the visual edge — header global search, inline edit, quick-input embedded in a card title row. Do not default to ghost.
- **Why** — Ghost removes the affordance (border / button). Without external context, users can't see "where to type."
- **How to apply** —
  - Use only inside header / card title row / modal inset where an outer frame supplies the boundary.
  - All form inputs → \`style=default\`, always.
  - Dropdown / date-picker / number-stepper variants → never ghost; the trailing icon is the core interaction and removing it breaks the control.

### 6. Do not translate tokens across sources
- **Rule** — The code-side \`llms.txt\` and Figma-side variables should hold the same values, but each side must read from its own source. Don't translate hex from \`llms.txt\` into Figma fills; don't treat a Figma variable's resolved value as the canonical code value.
- **Why** — The two sides can drift at any moment. Letting code read \`llms.txt\` and design read the Figma library is the only way both sides stay current.
- **How to apply** — Writing code: fetch \`llms.txt\`, reference tokens by name. Designing in Figma: \`search_design_system\` + \`importVariableByKeyAsync\` to bind variables.

---

## Logos

Brand logo assets for ArcSite. Use these when a design needs the product or
company mark — never re-trace, redraw, or recolor; reference the SVG directly.

Variants:
- **glyph** — icon-only mark for **light backgrounds**. Brand-orange glyph on transparent. Default for most product UI, headers, and marketing surfaces with light/white backgrounds.
  \`${SITE_BASE_URL}/logos/glyph.svg\`
- **glyph on dark** — icon-only mark for **dark backgrounds**. Light-treated glyph designed to remain legible on dark surfaces, photography, or busy backgrounds.
  \`${SITE_BASE_URL}/logos/glyph-on-dark.svg\`
- **glyph and text** — full wordmark + glyph for **light backgrounds**. Default lockup for headers, marketing pages, and product surfaces with light/white backgrounds.
  \`${SITE_BASE_URL}/logos/glyph-and-text.svg\`
- **glyph and text on dark** — full wordmark + glyph for **dark backgrounds**. Light-treated lockup; use on dark or photographic backgrounds where the default would lose contrast.
  \`${SITE_BASE_URL}/logos/glyph-and-text-on-dark.svg\`

Repo path: \`public/logos/\` (served at \`/logos/*.svg\`).

---

## Skills

LLM application guides for the ArcSite design system:

- [Design-to-code fidelity gate](${SITE_BASE_URL}/skills/arcsite-ds-apply/design-fidelity.md) *(read whenever visual fidelity = contract — the universal "read the design as a contract" rules: annotations first, fetch SVG don't hand-draw, variant diff. Plus the responsive-specific per-band variable protocol and the four-phase verification flow.)*
- [Canvas-app prototype guide](${SITE_BASE_URL}/skills/arcsite-ds-apply/canvas-prototype.md) *(read whenever surface shape = canvas-app, at any fidelity — coordinate-space contract, device-frame and pan/zoom architecture, counter-scaled stroke, \`ScreenAnchor\` pattern, canvas-specific verification matrix. Composes with the fidelity gate when also pixel-match.)*
- [Prototype mode guide](${SITE_BASE_URL}/skills/arcsite-ds-apply/prototype.md) *(standalone DS prototypes built from scratch. Building a **Design Playground** product-simulation prototype instead? The authoritative contract is the Playground's own: \`https://design-playground.arcsite.com/agent-instructions.md\`.)*
- [AntD conflict handling](${SITE_BASE_URL}/skills/arcsite-ds-apply/antd-conflict.md)
- [Figma MCP flow](${SITE_BASE_URL}/skills/arcsite-ds-apply/figma-flow.md)
`;

const outPath = resolve(ROOT, "public/llms.txt");
mkdirSync(dirname(outPath), { recursive: true });
writeFileSync(outPath, content, "utf-8");

console.log(
  `[llms.txt] wrote ${outPath} — ${content.length} chars, ${content.split("\n").length} lines`
);

const publicTokensDir = resolve(ROOT, "public/tokens");
mkdirSync(publicTokensDir, { recursive: true });

// Mirror the committed token markdown into public/tokens/ as a back-compat
// snapshot for the stable site URLs. NOTE: the live, canonical copies AI
// agents read are the Supabase Storage URLs in llms.txt (regenerated by the
// CMS on Publish — decision #12); these static mirrors can lag and exist only
// so the design-system.arcsite.com/tokens/*.md paths still resolve.
const TOKEN_DOCS = ["tokens-color.md", "tokens-typography.md", "tokens-size-space.md"];
for (const name of TOKEN_DOCS) {
  copyFileSync(resolve(ROOT, "tokens", name), resolve(publicTokensDir, name));
}
console.log(`[llms.txt] mirrored ${TOKEN_DOCS.length} token docs to public/tokens/`);

// bootstrap.css. PRODUCTION builds emit a 1-line @import shim forwarding to the
// live Supabase Storage copy the CMS regenerates on every Publish — so the
// stable design-system.arcsite.com/tokens/bootstrap.css URL always serves the
// latest tokens with no rebuild (decision #12). DEV / one-off runs emit the
// full inlined stylesheet from committed repo JSON so local work needs no
// network round-trip. (Egress trade-off of the shim is documented in #12.)
const bootstrapPath = resolve(publicTokensDir, "bootstrap.css");
let bootstrapCss;
if (PRODUCTION_BUILD) {
  bootstrapCss = buildBootstrapShim(`${STORAGE_BASE}/bootstrap.css`);
} else {
  bootstrapCss = buildBootstrapCss({
    globalColor,
    lightTokens,
    darkTokens,
    sizeGlobal,
    breakpoints,
    sizeWebMobile,
    sizeTabletDiff,
    sizeDesktopDiff,
    sizeDesktopLargeDiff,
    fontDesktop,
    breakpointPx,
    sizeDeviceMobileDiff,
    sizeDeviceTabletDiff,
    fontDeviceMobileDiff,
    fontDeviceTabletDiff,
  });
}
writeFileSync(bootstrapPath, bootstrapCss, "utf-8");
console.log(
  `[bootstrap.css] wrote ${bootstrapCss.length} chars (${PRODUCTION_BUILD ? "shim → Supabase" : "inline"})`
);

// breakpoints.js — emit the inlined module for the stable site URL. The live
// copy agents read is the Supabase Storage URL in llms.txt.
const breakpointsJs = buildBreakpointsJs(breakpointPx);
writeFileSync(resolve(publicTokensDir, "breakpoints.js"), breakpointsJs, "utf-8");
console.log(`[breakpoints.js] wrote ${breakpointsJs.length} chars`);
